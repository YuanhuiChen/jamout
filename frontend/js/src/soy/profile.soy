{namespace jamout.templates.Profile}

/**
 * Main Frame
 */
{template .frame}
<!-- TODO: ERROR HANDLING -->
 <div ng-controller="profileCtrl">
 	

   	<div class="row">
	
	
		<div class="col-xs-4 col-xs-offset-4">
			<div class="user-profile">
			  
					
					<a ng-href="/profile/{{'edit'}}" id="edit">Edit</a>
						
					    <!-- <img src="/images/admin.png"> User -->
																
					    
						<h1><strong ng-bind="welcome"></strong></h1>
						
						
						<h2> <strong ng-bind="about"></strong></h2>
						<h2><span ng-bind="location"></span></h2>
						<h2 id="url"><span ng-bind="url"></span></h2>
						<h2><span ng-bind="created"> </span></h2>
				     
		 	</div>
		 </div>
		 
    </div>
 </div>

    <!--- Broadcast form --> 
	  <div ng-controller="roomCtrl">
		<div class="row">
			<div class="col-xs-8 col-sm-4 col-md-4 col-lg-4 col-xs-offset-4">
		     
				<form class="broadcast-form">
				<fieldset>
				<h2 class="form-signin-heading"><strong>Let's get started</strong></h2>
				
				<p><strong>1. </strong>Enter text for your stream</p>
				
				<div class="control group">
				<input 
				name="title" 
				type="text" 
				class="form-control"  
				ng-model="roomModel.title" 
				placeholder="e.g. Producing music, thriving with the fam etc." 
				name="name">
				</div>

				<p><strong>2. </strong>Click "Stream your webcam" to start a live stream</p>

				<button type="submit" class="btn btn-lg btn-success-pink btn-block"  ng-click='create(roomModel)'/> Stream your webcam </button>


				</fieldset>
				</form>
			  
			</div>

		</div>
	</div>
			

{/template}
